# NL2SQL Engine — pinned dependencies
# LLM / Inference
openai>=1.30.0                  # Ollama OpenAI-compatible client

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Validation & settings
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Database
sqlalchemy>=2.0.30
asyncpg>=0.29.0                 # Async Postgres driver
aiosqlite>=0.20.0               # Async SQLite (testing / query log)
psycopg2-binary>=2.9.9          # Sync Postgres (introspection)

# Vector search (Windows-friendly alternative to faiss)
chromadb>=0.5.0

# Embeddings
sentence-transformers>=3.0.0

# Relationships
networkx>=3.3

# CLI
typer[all]>=0.12.0

# Config / YAML
pyyaml>=6.0.1
python-dotenv>=1.0.1

# SQL parsing (validator)
sqlparse>=0.5.0

# HTTP client (SDK)
httpx>=0.27.0

# Rate limiting helper
slowapi>=0.1.9

# Caching
diskcache>=5.6.3

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
